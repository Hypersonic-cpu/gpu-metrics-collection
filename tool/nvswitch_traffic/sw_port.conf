# sw_port —— 每端口(每根 NVLink)粒度：看跨卡流量落在哪些端口、热点分布
# CSV 下每端口一行（4 switch × 64 端口 = 256 行/拍），数据量大；分析热点用。
# 用法/字段含义见 tool/nvswitch_traffic/README.md 与 sw_switch.conf 注释。
level=port          # 每 switch 每端口
fields=rx,tx,total
switches=all        # 只看单台可改 0/1/2/3，省数据量
raw=0
