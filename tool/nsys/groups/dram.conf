# dram —— 第二遍：只看 HBM 读写带宽（读/写分开，比 dcgmi 1005 dram_active 细）
# ── Application scope ──
trace            = none
# ── System scope ──
sample           = none
cpuctxsw         = none
gpu_metrics_set  = file:sets/dram.config
gpu_metrics_freq = 10000
