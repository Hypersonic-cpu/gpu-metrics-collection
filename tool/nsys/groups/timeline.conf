# timeline —— GPU 硬件轨 + 全系统 CPU 采样/线程调度（看 CPU 与 GPU 怎么互相等）
# attach 也能用；system-wide 采样需 root（本工具自动加 sudo）。
# ── Application scope ──
trace            = none
# ── System scope ──
sample           = system-wide
cpuctxsw         = system-wide
gpu_metrics_set  = gh100
gpu_metrics_freq = 10000
